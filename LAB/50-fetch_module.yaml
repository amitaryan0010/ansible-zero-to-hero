---
- name: Learning fetch module
  hosts: target_host
  gather_facts: no
  
  tasks:
    - name: fetching the log file
      ansible.builtin.fetch:
        src: /var/log/messages
        dest: /tmp/{{ inventory_host }}.log
        flat: no