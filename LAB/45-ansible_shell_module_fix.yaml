---
- name: Learning Shell Module Behavior
  hosts: localhost
  gather_facts: no

  tasks:
    - name: Run a command using shell feature (*)
      ansible.builtin.shell: /usr/bin/ls -d /home/*
      register: ls_output
      changed_when: '"ansibleuser" not in ls_output.stdout'